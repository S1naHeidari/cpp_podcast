#include <iostream>
// #include "Podcast.h"
// #include "TestControl.h"
#include "Subscriber.h"
#include "Episode.h"
#include "Podcast.h"
using namespace std;

int main(){
	
	// TestControl* control = new TestControl();

	// control->launch();
	
	// delete control;


	// tests for Subscriber
	// Subscriber* s1 = new Subscriber("Sina Heidari", "6037697549015581");
	// string s_name;
	// s_name += "Sina Heidarii";
	//cout<<s_name<<endl;
	// s1->print();


	// tests for Episode
	// string info;
	// info += "sina ali mohammad";
	// string name;
	// name += "intro to ml podcast";
	// string content;
	// content += "gibberish";
	// Episode* e1 = new Episode(info, 1, name, content);
	// e1->play();

	//test for Podcast
	// string title, title2;
	// title += "AI podcast";
	// title2 += "BI podcast";
	// string host;
	// host += "sina ali mohammad";
	// Podcast* p1 = new Podcast(title, host);
	// Podcast* p2 = new Podcast(title2, host);
	//cout<<p1->getNumEpisodes()<<endl;
	// string added_title;
	// added_title += "AI podcast";
	// string added_content;
	// added_content += "AI podcast content";
	// p1->addEpisode(added_title, added_content);
	// added_title = "";
	// added_title += "ML podcast";
	// added_content = "";
	// added_content += "ML podcast content";
	// p1->addEpisode(added_title, added_content);
	// Episode *episode_return;
	//p1->getEpisode(1, &episode_return);
	//episode_return->play();
	//cout<<p1->lessThan(*p2)<<endl;
	// Podcast *p3 = p1;
	// cout<<p3->get_title()<<endl;
	// cout<<p3->get_host()<<endl;
	// p3->getEpisode(0, &episode_return);
	// episode_return->play();
	//p3 = p1;

	


	return 0;
}
